fastapi==0.111.0
uvicorn==0.30.1
sqlalchemy==2.0.31
# Postgres driver for cloud deploys (MALSCAN_DB_URL=postgresql://...);
# harmless locally where the SQLite default is used instead.
# 2.9.10+ required — earlier versions ship no Python 3.13 wheels.
psycopg2-binary==2.9.10
python-multipart==0.0.18
python-dotenv==1.1.0
requests==2.32.3
pefile==2024.8.26
playwright==1.60.0
# After installing, also run: playwright install chromium
# (headless Chromium used to render the HTML report to a real PDF — see
# GET /report/{job_id}/pdf in main.py)

# Threat intelligence (abuse.ch feeds — no key required)
# MalwareBazaar, ThreatFox, URLhaus clients use requests (already listed above)

# YARA scanning — install separately if on Windows:
#   pip install yara-python
# On Windows you may need Visual C++ build tools first:
#   https://visualstudio.microsoft.com/visual-cpp-build-tools/
# The engine runs fine without it — YARA will be skipped gracefully.
# yara-python>=4.3.0

# Optional: AbuseIPDB IP reputation
# No extra package needed — uses requests (already listed)
# Add ABUSEIPDB_API_KEY to backend/.env for IP reputation checks

# Document analysis uses only stdlib (zipfile, re, struct, math)
# No additional packages required for PDF/Office/OLE detection
